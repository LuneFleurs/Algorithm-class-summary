exchangeSort(a[])
    n <- len(a)
    for (i <- 0; i < n-1; i <- i+1) do {
        for (j <- i+1; j < n; j <- j+1) do {
            if(a[i] < a[j]) then {
                swap(a[i], a[j])
            }
        }
    }

end exchangeSort()